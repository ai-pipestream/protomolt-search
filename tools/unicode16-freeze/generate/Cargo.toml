[package]
name = "unicode16-freeze-generate"
version = "0.0.0"
edition = "2021"
publish = false

# A tool, not part of the product workspace; no dependencies.
[workspace]

[[bin]]
name = "generate"
path = "main.rs"
